apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: mango-admin
  namespace: mango-admin
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: "1Mi"
  volumeName:
  storageClassName: nfs-csi